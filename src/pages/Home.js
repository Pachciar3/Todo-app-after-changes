import React from "react";

import Typography from '@material-ui/core/Typography';

function Home() {
  return (
    <div>
      <Typography variant="h3">Welcome in Todo App</Typography>
      <Typography variant="subtitle1">Login and start using</Typography>
    </div>
  );
}

export default Home;
