import React from "react";

import Typography from '@material-ui/core/Typography';

function Home() {
  return (
    <div>
      <Typography variant="h3">Welcome in Todo App</Typography>
      <Typography variant="subtitle1">This is Todo app created with using React, redux and material-ui</Typography>
    </div>
  );
}

export default Home;
